const notFoundResource = 'Requested resource not found';
const noMatchingId = 'No user with matching ID found';
const emptyCredentials = 'Name, email or password should not be empty';
const incorrectCredentials = 'Incorrect email or password';
const existingUser = 'User with this email already exists';
const noMatchingCard = 'Card with matching ID was not found';
const notFoundCards = 'Cards were not found';
const denied = 'Permission denied';
const unAuthorized = 'Authorization required';
const notFoundUsers = 'No users were found';
const invalidUrl = 'Please provide valid url';

module.exports = {
  notFoundResource,
  noMatchingId,
  noMatchingCard,
  emptyCredentials,
  existingUser,
  notFoundCards,
  denied,
  unAuthorized,
  incorrectCredentials,
  notFoundUsers,
  invalidUrl,
};
